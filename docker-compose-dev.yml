services:
  game-web-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hello-phaser-dev
    deploy:
      resources:
        limits:
          memory: 2G
    ports:
      - "8090:8080"
    stdin_open: true
    tty: true
    volumes:
      - ./webgame:/webgame
